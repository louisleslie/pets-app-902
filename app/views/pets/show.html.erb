<h1><%= @pet.name %></h1>

<p>Species: <%= @pet.species %></p>

<p>Found at: <%= @pet.days_since_found %> Days ago</p>

<%= link_to "Delete", pet_path(@pet), data: {
    turbo_method: :delete, turbo_confirm: "Are you sure you want to delete poor #{@pet.name}?"
} %>